"use client";
import { useState } from "react";

export function BootstrapForm(){const [message,setMessage]=useState("");const [loading,setLoading]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setMessage("");const form=new FormData(e.currentTarget);const r=await fetch("/api/setup/bootstrap",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({secret:form.get("secret")})});const b=await r.json().catch(()=>({}));setLoading(false);setMessage(r.ok?"Database initialized. You can now sign in with ADMIN_EMAIL and ADMIN_PASSWORD.":b.error||"Setup failed");}return <form className="login-form" onSubmit={submit}><label>Bootstrap secret<input name="secret" type="password" required placeholder="Value from BOOTSTRAP_SECRET"/></label>{message&&<div className={message.startsWith("Database")?"form-success":"form-error"}>{message}</div>}<button className="btn-primary login-button" disabled={loading}>{loading?"Initializing…":"Initialize portal database"}</button></form>}
