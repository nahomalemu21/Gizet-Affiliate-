import { PayoutActions } from "@/components/admin/PayoutActions";
import { Icon } from "@/components/Icons";
import { PageHeader, StatusPill } from "@/components/UI";
import { listPayouts } from "@/lib/data";
import { formatEtb } from "@/lib/format";

export const dynamic="force-dynamic";
export default async function AdminPayouts(){const payouts=await listPayouts();const pending=payouts.filter(p=>p.status!=="paid").reduce((s,p)=>s+p.amount,0);return <div className="page-wrap"><PageHeader eyebrow="Finance control" title="Approve clean, auditable payouts." subtitle="Create payouts from the Creators page, then approve and mark them paid here."/><section className="admin-hero slim"><div><div className="hero-kicker"><Icon name="wallet" size={15}/> Outstanding creator payouts</div><div className="hero-amount">{formatEtb(pending)}</div></div><div className="admin-hero-metrics"><div><span>Schedule</span><strong>Twice monthly</strong></div><div><span>Minimum</span><strong>1,000 ETB</strong></div></div></section><div className="table-card"><table><thead><tr><th>Payout</th><th>Creator</th><th>Date</th><th>Orders</th><th>Method</th><th>Status</th><th>Amount</th><th>Action</th></tr></thead><tbody>{payouts.map(p=><tr key={p.id}><td><strong>{p.id}</strong><span>{p.transaction_reference||"No reference"}</span></td><td>{p.creator_name}</td><td>{new Date(p.created_at).toLocaleDateString()}</td><td>{p.order_count}</td><td>{p.method||"—"}</td><td><StatusPill status={p.status}/></td><td className="money-cell">{formatEtb(p.amount)}</td><td><PayoutActions id={p.id} status={p.status}/></td></tr>)}</tbody></table></div></div>}
